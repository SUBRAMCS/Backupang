import { CrossFieldMatcher } from './cross-field-matcher';

describe('CrossFieldMatcher', () => {
  it('should create an instance', () => {
    expect(new CrossFieldMatcher()).toBeTruthy();
  });
});
